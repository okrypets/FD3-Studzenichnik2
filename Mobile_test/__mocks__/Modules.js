// Импортируйте этот именованный объект в свой файл с тестом:
/*
export const mocksetName = jest.fn();
const mock = jest.fn().mockImplementation(() => {
    return {setName: mocksetName};
});

export default mock;


 */
